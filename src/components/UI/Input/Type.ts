/* eslint-disable no-shadow */
/* eslint-disable no-unused-vars */

export enum Type {
  TEXT = 'text',
  EMAIL = 'email',
  NUMBER = 'number',
  PASSWORD = 'password',
  DATE = 'date',
  DATETIME = 'datetime-local',
  TIME = 'time',
  IMAGE = 'image'
}
